# SKUIKIT 项目清理指南

> 本文档用于识别核心框架代码和样板代码，帮助你清理项目，保留必要的基础功能。

## 📊 项目统计

- **总文件数**: 227 个 TypeScript 文件
- **核心框架**: ~60 个文件
- **样板代码**: ~150 个文件
- **可选保留**: ~17 个文件

---

## 🎯 清理原则

### ✅ 必须保留（核心框架底座）
- API 交互管理（新增）
- 认证授权系统（新增）
- 国际化支持（新增）
- AG Grid 表格组件（新增）
- Shadcn UI 组件库
- 布局系统（侧边栏、头部）
- 主题系统
- 路由基础设施

### ❌ 可以删除（业务样板代码）
- 所有业务功能示例（dashboard, tasks, users, chats, apps）
- 对应的路由页面
- 示例数据文件

### 💡 可选保留
- 登录页面 UI（可能需要定制）
- 错误页面（401, 404, 500等）
- 配置抽屉、命令面板等工具组件

---

## 📁 详细清理清单

### 1. `src/lib/` - 核心库（✅ 全部保留）

| 目录/文件 | 用途 | 建议 |
|---------|------|------|
| `lib/api/` | **HTTP客户端封装**（新增核心功能） | ✅ 保留 - 必需 |
| `├── client.ts` | Axios 客户端实例 | ✅ 保留 |
| `├── config.ts` | API 端点配置 | ✅ 保留 |
| `├── interceptors.ts` | 请求/响应拦截器 | ✅ 保留 |
| `└── types.ts` | API 类型定义 | ✅ 保留 |
| `lib/auth/` | **认证授权系统**（新增核心功能） | ✅ 保留 - 必需 |
| `├── auth-service.ts` | 认证服务（登录、登出、刷新） | ✅ 保留 |
| `├── auth-store.ts` | Zustand 认证状态管理 | ✅ 保留 |
| `├── storage.ts` | Token 本地存储 | ✅ 保留 |
| `└── types.ts` | 认证类型定义 | ✅ 保留 |
| `lib/i18n/` | **国际化支持**（新增核心功能） | ✅ 保留 - 必需 |
| `├── config.ts` | i18next 配置 | ✅ 保留 |
| `├── hooks.ts` | useI18n hook | ✅ 保留 |
| `├── locales/en.json` | 英文语言包 | ✅ 保留 |
| `└── locales/zh.json` | 中文语言包 | ✅ 保留 |
| `lib/utils.ts` | 工具函数（cn等） | ✅ 保留 |
| `lib/cookies.ts` | Cookie 操作 | 💡 可选 - 看业务需求 |
| `lib/handle-server-error.ts` | 错误处理 | 💡 可选 |
| `lib/show-submitted-data.tsx` | 调试工具 | ❌ 删除 - 仅用于示例 |

---

### 2. `src/components/` - 组件库

#### 2.1 `components/ui/` - Shadcn UI（✅ 全部保留）

| 组件 | 用途 | 建议 |
|-----|------|------|
| `ui/button.tsx` | 按钮组件 | ✅ 保留 - 基础组件 |
| `ui/card.tsx` | 卡片组件 | ✅ 保留 |
| `ui/dialog.tsx` | 对话框 | ✅ 保留 |
| `ui/input.tsx` | 输入框 | ✅ 保留 |
| `ui/select.tsx` | 选择器 | ✅ 保留 |
| `ui/table.tsx` | 表格组件 | ✅ 保留 |
| `ui/form.tsx` | 表单组件 | ✅ 保留 |
| `ui/*` | 其他所有 UI 组件 | ✅ 保留 - 共31个组件 |

#### 2.2 `components/auth/` - 认证组件（✅ 保留 - 新增）

| 文件 | 用途 | 建议 |
|-----|------|------|
| `protected-route.tsx` | 路由守卫 | ✅ 保留 - 核心功能 |
| `permission-guard.tsx` | 权限控制 | ✅ 保留 - 核心功能 |

#### 2.3 `components/i18n/` - 国际化组件（✅ 保留 - 新增）

| 文件 | 用途 | 建议 |
|-----|------|------|
| `language-switcher.tsx` | 语言切换器 | ✅ 保留 - 核心功能 |

#### 2.4 `components/data-table/` - 表格组件

| 文件 | 用途 | 建议 |
|-----|------|------|
| `ag-grid-table.tsx` | **AG Grid 封装**（新增核心功能） | ✅ 保留 - 必需 |
| `index.ts` | 导出文件 | ✅ 保留 |
| `column-header.tsx` | TanStack Table 列头 | ❌ 删除 - 我们用AG Grid |
| `faceted-filter.tsx` | TanStack Table 过滤器 | ❌ 删除 |
| `pagination.tsx` | TanStack Table 分页 | ❌ 删除 |
| `toolbar.tsx` | TanStack Table 工具栏 | ❌ 删除 |
| `view-options.tsx` | TanStack Table 视图选项 | ❌ 删除 |
| `bulk-actions.tsx` | TanStack Table 批量操作 | ❌ 删除 |

#### 2.5 `components/layout/` - 布局系统（✅ 大部分保留）

| 文件 | 用途 | 建议 |
|-----|------|------|
| `authenticated-layout.tsx` | 认证后的主布局 | ✅ 保留 - 核心布局 |
| `app-sidebar.tsx` | 应用侧边栏 | ✅ 保留 - 核心布局 |
| `header.tsx` | 顶部导航 | ✅ 保留 - 核心布局 |
| `main.tsx` | 主内容区 | ✅ 保留 |
| `top-nav.tsx` | 顶部导航组件 | ✅ 保留 |
| `nav-group.tsx` | 导航分组 | ✅ 保留 |
| `nav-user.tsx` | 用户导航菜单 | ✅ 保留 |
| `app-title.tsx` | 应用标题 | ✅ 保留 |
| `team-switcher.tsx` | 团队切换器 | 💡 可选 - 多租户才需要 |
| `data/sidebar-data.ts` | 侧边栏菜单数据 | ✅ 保留 - 但需修改为你的菜单 |
| `types.ts` | 布局类型定义 | ✅ 保留 |

#### 2.6 `components/` 其他工具组件

| 文件 | 用途 | 建议 |
|-----|------|------|
| `theme-switch.tsx` | 主题切换 | ✅ 保留 - 常用功能 |
| `command-menu.tsx` | 命令面板（Cmd+K） | 💡 可选 - 很实用 |
| `config-drawer.tsx` | 配置抽屉 | 💡 可选 - 调试工具 |
| `profile-dropdown.tsx` | 个人资料下拉 | ✅ 保留 - 常用 |
| `search.tsx` | 搜索组件 | 💡 可选 |
| `navigation-progress.tsx` | 路由进度条 | ✅ 保留 - 用户体验好 |
| `date-picker.tsx` | 日期选择器 | ✅ 保留 - 常用 |
| `password-input.tsx` | 密码输入框 | ✅ 保留 - 登录用 |
| `select-dropdown.tsx` | 选择下拉 | 💡 可选 |
| `sign-out-dialog.tsx` | 登出确认 | 💡 可选 |
| `confirm-dialog.tsx` | 确认对话框 | ✅ 保留 - 常用 |
| `skip-to-main.tsx` | 无障碍跳转 | 💡 可选 - 无障碍功能 |
| `coming-soon.tsx` | 敬请期待页面 | ❌ 删除 - 占位页面 |
| `learn-more.tsx` | 学习更多链接 | ❌ 删除 - 示例组件 |
| `long-text.tsx` | 长文本显示 | ❌ 删除 - 示例组件 |

---

### 3. `src/features/` - 业务功能（大部分删除）

| 目录 | 用途 | 大小 | 建议 |
|-----|------|------|------|
| `features/auth/` | 登录/注册页面UI | 940K | 💡 **可选** - 可能需要定制UI |
| `├── sign-in/` | 登录页面（2个版本） | - | 💡 保留1个，删除另一个 |
| `├── sign-up/` | 注册页面 | - | 💡 根据需求决定 |
| `├── forgot-password/` | 忘记密码 | - | 💡 根据需求决定 |
| `├── otp/` | OTP验证 | - | 💡 根据需求决定 |
| `└── auth-layout.tsx` | 认证页面布局 | - | 💡 如果保留登录页就保留 |
| `features/errors/` | 错误页面 | 20K | 💡 **建议保留** - 通用错误页 |
| `├── not-found-error.tsx` | 404页面 | - | ✅ 保留 |
| `├── unauthorized-error.tsx` | 401页面 | - | ✅ 保留 |
| `├── forbidden.tsx` | 403页面 | - | ✅ 保留 |
| `├── general-error.tsx` | 500页面 | - | ✅ 保留 |
| `└── maintenance-error.tsx` | 503维护页面 | - | ✅ 保留 |
| `features/dashboard/` | 仪表盘示例 | 28K | ❌ **删除** - 业务样板 |
| `features/tasks/` | 任务管理示例 | 68K | ❌ **删除** - 业务样板 |
| `features/users/` | 用户管理示例 | 80K | ❌ **删除** - 业务样板 |
| `features/chats/` | 聊天功能示例 | 40K | ❌ **删除** - 业务样板 |
| `features/apps/` | 应用列表示例 | 12K | ❌ **删除** - 业务样板 |
| `features/settings/` | 设置页面示例 | 68K | ❌ **删除** - 业务样板 |

**删除建议**: 可直接删除 `dashboard/`, `tasks/`, `users/`, `chats/`, `apps/`, `settings/` 共 ~296K

---

### 4. `src/routes/` - 路由页面

| 路由 | 用途 | 建议 |
|-----|------|------|
| `__root.tsx` | 根路由 | ✅ 保留 - 必需 |
| `demo.tsx` | **SKUIKIT功能演示**（新增） | ✅ 保留 - 核心示例 |
| `(auth)/sign-in.tsx` | 登录路由 | 💡 可选 - 配合 features/auth |
| `(auth)/sign-in-2.tsx` | 登录路由版本2 | ❌ 删除 - 重复 |
| `(auth)/sign-up.tsx` | 注册路由 | 💡 可选 |
| `(auth)/forgot-password.tsx` | 忘记密码路由 | 💡 可选 |
| `(auth)/otp.tsx` | OTP验证路由 | 💡 可选 |
| `(errors)/404.tsx` | 404错误页路由 | ✅ 保留 |
| `(errors)/401.tsx` | 401错误页路由 | ✅ 保留 |
| `(errors)/403.tsx` | 403错误页路由 | ✅ 保留 |
| `(errors)/500.tsx` | 500错误页路由 | ✅ 保留 |
| `(errors)/503.tsx` | 503错误页路由 | ✅ 保留 |
| `_authenticated/route.tsx` | 认证布局路由 | ✅ 保留 - 核心 |
| `_authenticated/index.tsx` | 首页（仪表盘） | 💡 改为你的首页 |
| `_authenticated/tasks/` | 任务管理路由 | ❌ 删除 |
| `_authenticated/users/` | 用户管理路由 | ❌ 删除 |
| `_authenticated/chats/` | 聊天路由 | ❌ 删除 |
| `_authenticated/apps/` | 应用列表路由 | ❌ 删除 |
| `_authenticated/settings/` | 设置路由 | ❌ 删除 |
| `_authenticated/help-center/` | 帮助中心路由 | ❌ 删除 |
| `_authenticated/errors/` | 动态错误页 | 💡 可选 |
| `clerk/` | Clerk认证相关路由 | ❌ 删除 - 我们用自己的认证 |

---

### 5. `src/context/` - 上下文（✅ 大部分保留）

| 文件 | 用途 | 建议 |
|-----|------|------|
| `theme-provider.tsx` | 主题上下文 | ✅ 保留 - 核心功能 |
| `layout-provider.tsx` | 布局配置上下文 | ✅ 保留 |
| `direction-provider.tsx` | RTL/LTR方向 | 💡 可选 - 需要RTL才保留 |
| `font-provider.tsx` | 字体配置 | 💡 可选 |
| `search-provider.tsx` | 搜索上下文 | 💡 可选 - 配合命令面板 |

---

### 6. `src/hooks/` - 自定义Hooks

| 文件 | 用途 | 建议 |
|-----|------|------|
| `use-mobile.tsx` | 移动端检测 | ✅ 保留 - 响应式常用 |
| `use-dialog-state.tsx` | 对话框状态管理 | ✅ 保留 - 通用hook |
| `use-table-url-state.ts` | 表格URL状态同步 | ❌ 删除 - 仅用于TanStack Table |

---

### 7. `src/assets/` - 资源文件

| 目录 | 用途 | 建议 |
|-----|------|------|
| `logo.tsx` | Logo组件 | ✅ 保留 - 改为你的Logo |
| `brand-icons/` | 品牌图标（16个） | ❌ 删除 - 示例图标 |
| `custom/` | 自定义图标 | ✅ 保留 - 布局、主题图标 |
| `clerk-*.tsx` | Clerk相关图标 | ❌ 删除 - 不用Clerk |

---

### 8. `src/stores/` - 状态管理

| 文件 | 用途 | 建议 |
|-----|------|------|
| `auth-store.ts` | 旧的认证store | ❌ 删除 - 已被 lib/auth 替代 |

---

### 9. `src/config/` - 配置

| 文件 | 用途 | 建议 |
|-----|------|------|
| `fonts.ts` | 字体配置 | 💡 可选 - 看是否需要多字体 |

---

### 10. `src/styles/` - 样式（✅ 全部保留）

| 文件 | 用途 | 建议 |
|-----|------|------|
| `index.css` | 全局样式 | ✅ 保留 - 必需 |
| `theme.css` | 主题变量 | ✅ 保留 - 必需 |

---

## 🚀 推荐清理步骤

### 第一阶段：删除明确的样板代码

```bash
# 1. 删除业务功能示例
rm -rf src/features/dashboard
rm -rf src/features/tasks
rm -rf src/features/users
rm -rf src/features/chats
rm -rf src/features/apps
rm -rf src/features/settings

# 2. 删除对应的路由
rm -rf src/routes/_authenticated/tasks
rm -rf src/routes/_authenticated/users
rm -rf src/routes/_authenticated/chats
rm -rf src/routes/_authenticated/apps
rm -rf src/routes/_authenticated/settings
rm -rf src/routes/_authenticated/help-center
rm -rf src/routes/clerk

# 3. 删除不需要的认证路由
rm src/routes/(auth)/sign-in-2.tsx

# 4. 删除 TanStack Table 相关组件
rm src/components/data-table/column-header.tsx
rm src/components/data-table/faceted-filter.tsx
rm src/components/data-table/pagination.tsx
rm src/components/data-table/toolbar.tsx
rm src/components/data-table/view-options.tsx
rm src/components/data-table/bulk-actions.tsx

# 5. 删除示例组件
rm src/components/coming-soon.tsx
rm src/components/learn-more.tsx
rm src/components/long-text.tsx

# 6. 删除示例资源
rm -rf src/assets/brand-icons
rm src/assets/clerk-*.tsx

# 7. 删除旧的store
rm src/stores/auth-store.ts

# 8. 删除示例工具
rm src/lib/show-submitted-data.tsx

# 9. 删除 TanStack Table hook
rm src/hooks/use-table-url-state.ts
```

### 第二阶段：可选删除（根据需求）

```bash
# 如果不需要登录UI，删除
rm -rf src/features/auth
rm -rf src/routes/(auth)

# 如果不需要 RTL 支持
rm src/context/direction-provider.tsx

# 如果不需要多字体
rm src/context/font-provider.tsx
rm src/config/fonts.ts

# 如果不需要团队切换
rm src/components/layout/team-switcher.tsx
```

### 第三阶段：清理后需要修改的文件

清理完成后，需要修改以下文件：

1. **`src/components/layout/data/sidebar-data.ts`**
   - 修改侧边栏菜单为你的业务菜单
   - 删除指向已删除页面的链接

2. **`src/routes/_authenticated/index.tsx`**
   - 改为你的实际首页
   - 或者重定向到 `/demo`

3. **`src/assets/logo.tsx`**
   - 替换为你的 Logo

4. **`src/routeTree.gen.ts`**
   - 删除文件后重新生成：`npm run dev`

5. **清理导入错误**
   - 运行 `npm run lint` 检查
   - 删除对已删除文件的导入

---

## 📈 清理后的项目结构

```
src/
├── lib/                          # ✅ 核心库（API、Auth、i18n）
├── components/
│   ├── ui/                       # ✅ 31个 Shadcn 组件
│   ├── auth/                     # ✅ 路由守卫、权限控制
│   ├── i18n/                     # ✅ 语言切换器
│   ├── data-table/
│   │   ├── ag-grid-table.tsx    # ✅ AG Grid 封装
│   │   └── index.ts
│   ├── layout/                   # ✅ 布局系统
│   └── [工具组件]                # ✅ 根据需求保留
├── context/                      # ✅ 主题、布局上下文
├── hooks/                        # ✅ 通用 hooks
├── features/
│   ├── auth/                     # 💡 可选 - 登录UI
│   └── errors/                   # ✅ 错误页面
├── routes/
│   ├── __root.tsx               # ✅ 根路由
│   ├── demo.tsx                 # ✅ 功能演示
│   ├── (auth)/                  # 💡 可选 - 登录路由
│   ├── (errors)/                # ✅ 错误路由
│   └── _authenticated/
│       ├── route.tsx            # ✅ 认证布局
│       └── index.tsx            # ✅ 首页（需修改）
└── styles/                      # ✅ 全局样式
```

---

## 🎯 清理后的框架能力

保留核心功能后，SKUIKIT 仍然具备：

### ✅ 完整的核心功能

1. **API 交互**
   - Axios 客户端
   - 请求/响应拦截器
   - 自动 Token 管理
   - 错误处理

2. **认证授权**
   - 登录/登出
   - Token 刷新
   - 路由守卫
   - 权限控制

3. **国际化**
   - 中英文切换
   - 易于扩展新语言
   - 语言切换组件

4. **数据表格**
   - AG Grid Community
   - 排序、过滤、分页
   - 自定义渲染
   - 主题适配

5. **UI 组件库**
   - 31个 Shadcn UI 组件
   - 亮/暗主题
   - 响应式设计
   - 完整的布局系统

### ✅ 开发体验

- TypeScript 全覆盖
- ESLint + Prettier
- Vite 快速构建
- TanStack Router
- Zustand 状态管理
- React Hook Form + Zod

---

## 📝 注意事项

1. **删除前备份**
   - 建议先提交当前状态：`git commit -am "backup before cleanup"`
   - 或创建分支：`git checkout -b backup-before-cleanup`

2. **渐进式清理**
   - 建议先删除第一阶段（明确的样板代码）
   - 测试运行：`npm run dev`
   - 确认无误后再进行第二阶段

3. **保留文档**
   - 本清理指南请保留在项目根目录
   - 方便后续参考和团队协作

4. **更新路由**
   - 删除路由后需要重新生成 `routeTree.gen.ts`
   - 启动开发服务器会自动生成

---

## 🔍 验证清理结果

清理完成后，运行以下命令验证：

```bash
# 1. 检查 TypeScript 编译
npm run build

# 2. 检查代码规范
npm run lint

# 3. 启动开发服务器
npm run dev

# 4. 访问演示页面
# http://localhost:5173/demo
```

如果以上都正常，说明清理成功！✨

---

## 📚 相关文档

- [FRAMEWORK.md](./FRAMEWORK.md) - 框架使用文档
- [AG_GRID_DEMO.md](./AG_GRID_DEMO.md) - AG Grid 演示说明
- [.claude/README.md](./.claude/README.md) - AI 辅助开发指南

---

**最后更新**: 2025-10-15
**维护者**: SKUIKIT Team
