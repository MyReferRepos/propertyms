# 工作台 (Workbench) 设计分析

> 版本: 1.0
> 创建日期: 2025-12-17
> 状态: 设计分析完成

---

## 一、概述

本文档分析 PropertyMS 系统中工作台 (Workbench) 功能的设计，重点探讨：
1. Reminder（提醒）与 Todo/Task List（任务列表）功能是否应该合并
2. 未读及未处理消息的展示方案
3. 完整的 UI 原型设计

---

## 二、Reminder vs Todo/Task List 对比分析

### 2.1 功能对比

| 维度 | Reminder (提醒) | Todo/Task (任务) |
|------|----------------|------------------|
| **触发机制** | 时间驱动，被动提醒 | 主动创建，需要执行 |
| **生命周期** | 可循环（每天/周/月/年） | 通常一次性 |
| **交互方式** | 查看/推迟/关闭 | 开始/进行中/完成 |
| **进度状态** | 无进度概念 | 有明确进度 |
| **典型场景** | "租约将在30天后到期" | "审核Unit 5A的租客申请" |
| **关联性** | 常常触发任务 | 可能来源于提醒 |

### 2.2 相似之处

1. 都是需要用户关注的行动项
2. 都有时间维度（到期日、截止日）
3. 都可以标记为已完成/已处理
4. 都可以关联到特定实体（房产、租约、租客等）
5. 都作为用户的记忆辅助工具

### 2.3 关键差异

**Reminder 特有：**
- 循环机制（每日/周/月/年/自定义）
- 推迟（Snooze）功能
- 系统自动生成（基于业务规则）
- 永远不会真正"完成"（循环提醒）

**Task 特有：**
- 明确的状态流转（待处理 → 进行中 → 完成）
- 子任务支持
- 可分配给他人
- 进度追踪

### 2.4 决策：不完全合并，统一展示

**核心理念：**
```
提醒(Reminder) + 任务(Task) = 行动项(Action Item)
```

**理由：**
1. **心智模型不同** - 用户对"提醒我"和"完成这件事"有本质区别认知
2. **交互模式不同** - 循环提醒永远不会"完成"，与任务的生命周期不同
3. **但工作流关联紧密** - 提醒常常转化为任务，应支持一键转化

**推荐方案：统一工作台 + 逻辑分层**
- 在同一个工作台界面展示所有行动项
- 通过类型标签和筛选器区分
- 支持提醒转任务的快捷操作

---

## 三、数据模型设计

### 3.1 统一行动项基础类型

```typescript
// 行动项基础类型
interface ActionItemBase {
  id: string
  title: string
  description?: string
  priority: 'low' | 'medium' | 'high' | 'urgent'
  dueAt?: Date
  snoozedUntil?: Date

  // 关联实体 - 房产管理特有
  relatedEntity?: {
    type: 'property' | 'tenant' | 'tenancy' | 'maintenance' | 'compliance' | 'inspection'
    id: string
    name: string
  }

  // 元数据
  createdAt: Date
  updatedAt: Date
  createdBy: string
}
```

### 3.2 任务类型 (Task)

```typescript
interface Task extends ActionItemBase {
  itemType: 'task'
  status: 'pending' | 'in_progress' | 'completed' | 'cancelled'
  completedAt?: Date
  assignee?: string
  subtasks?: SubTask[]
  category: 'maintenance' | 'leasing' | 'compliance' | 'financial' | 'general'
}

interface SubTask {
  id: string
  title: string
  completed: boolean
}
```

### 3.3 提醒类型 (Reminder)

```typescript
interface Reminder extends ActionItemBase {
  itemType: 'reminder'
  status: 'active' | 'snoozed' | 'dismissed'

  // 循环设置
  recurrence?: {
    type: 'once' | 'daily' | 'weekly' | 'monthly' | 'yearly' | 'custom'
    interval?: number       // 每隔N个周期
    daysOfWeek?: number[]   // 周几 [1,3,5] = 周一三五
    dayOfMonth?: number     // 每月几号
    endDate?: Date          // 循环结束日期
    occurrences?: number    // 总循环次数
  }

  // 触发来源
  source: 'manual' | 'system' | 'workflow'

  // 转化能力
  convertedToTaskId?: string
}
```

### 3.4 消息类型 (Message)

```typescript
interface Message {
  id: string
  type: 'tenant' | 'owner' | 'supplier' | 'system' | 'team'
  subject: string
  content: string
  preview: string  // 内容预览（前100字符）
  sender: {
    id: string
    name: string
    avatar?: string
  }

  // 状态
  status: 'unread' | 'read' | 'archived'
  requiresAction: boolean
  actionTakenAt?: Date

  // 关联
  relatedEntity?: {
    type: string
    id: string
    name: string
  }
  threadId?: string  // 会话线程

  // 元数据
  receivedAt: Date
  readAt?: Date
}
```

### 3.5 统一类型定义

```typescript
type ActionItem = Task | Reminder
type WorkbenchItem = ActionItem | Message
```

---

## 四、UI 设计方案

### 4.1 整体布局

```
┌─────────────────────────────────────────────────────────────────┐
│  工作台 (Workbench)                              [+ 新建 ▼]     │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 今日聚焦 (Today's Focus) - 智能优先级排序                     ││
│  │ ───────────────────────────────────────────────────────────  ││
│  │ 🔴 逾期: 2项  |  ⚠️ 今日到期: 5项  |  📅 本周: 12项          ││
│  └─────────────────────────────────────────────────────────────┘│
│                                                                 │
│  ┌──────────────────────────┬──────────────────────────────────┐│
│  │ 行动项 (Action Items)    │ 消息 (Messages)                   ││
│  │ ────────────────────────  │ ────────────────────────────────  ││
│  │ [全部] [任务] [提醒]      │ [全部] [未读] [待处理]            ││
│  │                          │                                  ││
│  │ ☑ 待办任务 (Tasks)       │ 👤 租客消息                       ││
│  │   ├─ 审核租客申请        │ 👔 业主消息                       ││
│  │   └─ 安排维修检查        │ 🔔 系统通知                       ││
│  │                          │ 🏢 供应商消息                     ││
│  │ ⏰ 提醒 (Reminders)      │                                  ││
│  │   ├─ 租约到期 (30天)     │                                  ││
│  │   └─ 合规检查 (每季度)   │                                  ││
│  └──────────────────────────┴──────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 今日聚焦组件 (Today's Focus)

智能聚合当日最重要的项目：

```typescript
interface TodayFocus {
  overdue: ActionItem[]      // 已逾期
  dueToday: ActionItem[]     // 今日到期
  urgent: ActionItem[]       // 紧急优先级
  unreadMessages: number     // 未读消息数
  requiresAction: number     // 待处理消息数
}
```

**展示规则：**
1. 逾期项优先显示（红色标签）
2. 今日到期次之（橙色标签）
3. 紧急优先级再次（黄色标签）
4. 最多显示 5 项，其余折叠

### 4.3 行动项列表 (Action Items)

**筛选选项：**
- 全部 / 任务 / 提醒
- 时间范围：逾期 / 今日 / 本周 / 更晚
- 优先级：紧急 / 高 / 中 / 低
- 关联实体：房产 / 租约 / 租客 / 维护

**任务项展示：**
```
┌────────────────────────────────────────────────┐
│ ☐ 审核租客申请 - John Smith                    │
│   🏠 Unit 5A, 123 Queen St    🔴 高优先级      │
│   📅 到期: 今天              [开始] [推迟]      │
└────────────────────────────────────────────────┘
```

**提醒项展示：**
```
┌────────────────────────────────────────────────┐
│ ⏰ 租约到期提醒                                 │
│   🏠 Unit 3B - 租约将在30天后到期              │
│   🔄 单次提醒               [转为任务] [推迟]   │
└────────────────────────────────────────────────┘
```

### 4.4 消息列表 (Messages)

**筛选选项：**
- 全部 / 未读 / 待处理 / 已归档
- 来源：租客 / 业主 / 供应商 / 系统 / 团队

**消息项展示：**
```
┌────────────────────────────────────────────────┐
│ 👤 Sarah Chen (租客)                   10:30   │
│   主题: 维修请求 - 热水器故障                   │
│   "您好，我们单位的热水器今早开始..."           │
│   🏠 Unit 2A    ⚡ 需要处理                     │
│   [回复] [转为任务] [标记已读]                  │
└────────────────────────────────────────────────┘
```

### 4.5 快捷操作

**新建菜单：**
- 新建任务
- 设置提醒
- 发送消息

**项目操作：**
- 任务：开始 / 完成 / 取消 / 推迟 / 删除
- 提醒：转为任务 / 推迟 / 关闭 / 编辑循环
- 消息：回复 / 转为任务 / 设置提醒 / 归档

**智能转化：**
```
提醒 ──[转为任务]──→ 任务
消息 ──[转为任务]──→ 任务
消息 ──[设置提醒]──→ 提醒
```

---

## 五、房产管理典型场景

### 5.1 系统自动提醒

| 场景 | 类型 | 来源 | 循环 | 提前时间 |
|------|------|------|------|----------|
| 租约到期提醒 | 提醒 | 系统 | 单次 | 90/60/30天 |
| 每月收租日 | 提醒 | 系统 | 每月 | 当天 |
| 年度房产检查 | 提醒 | 系统 | 每年 | 30天前 |
| 合规证书到期 | 提醒 | 系统 | 单次 | 60天前 |
| 保险续期 | 提醒 | 系统 | 每年 | 30天前 |

### 5.2 用户手动任务

| 场景 | 类型 | 来源 | 优先级 |
|------|------|------|--------|
| 审核新租客申请 | 任务 | 手动/消息转化 | 高 |
| 处理维修请求 | 任务 | 系统自动 | 取决于类型 |
| 准备租约续签文件 | 任务 | 提醒转化 | 中 |
| 安排房产检查 | 任务 | 提醒转化 | 中 |
| 跟进欠租催缴 | 任务 | 系统自动 | 高 |

### 5.3 消息类型

| 来源 | 典型内容 | 需要操作 |
|------|----------|----------|
| 租客 | 维修请求、投诉、咨询 | 是 |
| 业主 | 询问报告、审批请求 | 是 |
| 供应商 | 报价、工期安排、完工通知 | 是 |
| 系统 | 付款到账、状态变更、警告 | 可能 |
| 团队 | 内部沟通、任务分配 | 是 |

---

## 六、组件结构设计

```
src/features/workbench/
├── pages/
│   └── workbench-page.tsx          # 主工作台页面
├── components/
│   ├── today-focus/
│   │   ├── today-focus-card.tsx    # 今日聚焦卡片
│   │   └── focus-item.tsx          # 聚焦项
│   ├── action-items/
│   │   ├── action-item-list.tsx    # 行动项列表
│   │   ├── task-item.tsx           # 任务项
│   │   ├── reminder-item.tsx       # 提醒项
│   │   ├── task-form.tsx           # 任务表单
│   │   └── reminder-form.tsx       # 提醒表单
│   ├── messages/
│   │   ├── message-list.tsx        # 消息列表
│   │   ├── message-item.tsx        # 消息项
│   │   └── message-detail.tsx      # 消息详情
│   └── shared/
│       ├── priority-badge.tsx      # 优先级徽章
│       ├── entity-link.tsx         # 关联实体链接
│       ├── due-date-badge.tsx      # 到期日徽章
│       └── snooze-picker.tsx       # 推迟选择器
├── hooks/
│   ├── use-action-items.ts         # 行动项数据Hook
│   ├── use-messages.ts             # 消息数据Hook
│   └── use-today-focus.ts          # 今日聚焦Hook
├── types/
│   └── workbench.ts                # 类型定义
└── locales/
    ├── en.json
    └── zh-CN.json
```

---

## 七、循环提醒配置示例

### 7.1 每月收租提醒

```typescript
const monthlyRentReminder: Reminder = {
  id: 'rem-rent-monthly',
  itemType: 'reminder',
  title: '月度收租日',
  description: '检查所有房产的租金到账情况',
  priority: 'high',
  status: 'active',
  recurrence: {
    type: 'monthly',
    dayOfMonth: 1  // 每月1号
  },
  source: 'system',
  createdAt: new Date(),
  updatedAt: new Date(),
  createdBy: 'system'
}
```

### 7.2 季度合规检查提醒

```typescript
const quarterlyComplianceReminder: Reminder = {
  id: 'rem-compliance-quarterly',
  itemType: 'reminder',
  title: '季度合规检查',
  description: '检查所有房产的Healthy Homes合规状态',
  priority: 'medium',
  status: 'active',
  recurrence: {
    type: 'monthly',
    interval: 3,     // 每3个月
    dayOfMonth: 1    // 每月1号
  },
  source: 'system',
  relatedEntity: {
    type: 'compliance',
    id: 'all',
    name: '全部房产'
  },
  createdAt: new Date(),
  updatedAt: new Date(),
  createdBy: 'system'
}
```

### 7.3 工作日提醒

```typescript
const weekdayReminder: Reminder = {
  id: 'rem-daily-check',
  itemType: 'reminder',
  title: '每日任务检查',
  description: '检查今日待处理事项',
  priority: 'low',
  status: 'active',
  recurrence: {
    type: 'weekly',
    daysOfWeek: [1, 2, 3, 4, 5]  // 周一到周五
  },
  source: 'manual',
  createdAt: new Date(),
  updatedAt: new Date(),
  createdBy: 'user-001'
}
```

---

## 八、技术实现要点

### 8.1 数据持久化

当前 MVP 阶段使用 localStorage + Mock 数据：
- 用户创建的任务/提醒存储在 localStorage
- 系统提醒基于 Mock 数据生成
- 消息数据使用 Mock 模拟

### 8.2 状态管理

使用 Zustand 管理工作台状态：
```typescript
interface WorkbenchStore {
  tasks: Task[]
  reminders: Reminder[]
  messages: Message[]

  // Actions
  addTask: (task: Omit<Task, 'id' | 'createdAt' | 'updatedAt'>) => void
  updateTask: (id: string, updates: Partial<Task>) => void
  addReminder: (reminder: Omit<Reminder, 'id' | 'createdAt' | 'updatedAt'>) => void
  snoozeItem: (id: string, until: Date) => void
  convertReminderToTask: (reminderId: string) => Task
  markMessageRead: (id: string) => void
}
```

### 8.3 i18n 支持

所有用户可见文本使用 `t()` 函数：
```typescript
// 命名空间: workbench
t('workbench.todayFocus.title')
t('workbench.actionItems.tasks')
t('workbench.actionItems.reminders')
t('workbench.messages.unread')
```

---

## 九、结论与建议

### 9.1 核心决策

| 决策点 | 建议 |
|--------|------|
| **合并 Reminder 和 Task?** | ❌ 不完全合并，但在同一工作台展示 |
| **数据模型** | 统一基础类型 + 扩展字段区分 |
| **展示方式** | 单一时间线，可按类型过滤 |
| **消息系统** | 独立但集成，支持转化为任务/提醒 |

### 9.2 设计价值

1. **概念清晰** - 用户理解什么是提醒、什么是任务
2. **操作统一** - 在同一界面管理所有行动项
3. **灵活转化** - 提醒可转任务，消息可转任务/提醒
4. **智能聚合** - 今日聚焦让用户快速了解当务之急

### 9.3 后续扩展

- 推送通知（Web Push / Email / SMS）
- 日历视图
- 团队协作（任务分配、评论）
- 工作流自动化
- 与外部日历同步（Google Calendar, Outlook）

---

**文档版本**: 1.0
**创建日期**: 2025-12-17
**状态**: 设计分析完成
**维护者**: Development Team
