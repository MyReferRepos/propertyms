# PropertyMS 路演MVP重构计划

**目标**: 2天内完成可用于路演的Demo
**策略**: 保留基础设施，删除复杂功能，重点实现6大差异化功能

---

## 📁 代码清理计划

### ✅ 保留的模块

#### 1. 核心基础设施
- `/src/components/ui/*` - shadcn/ui组件（完整保留）
- `/src/lib/i18n/*` - 国际化系统（简化）
- `/src/context/*` - 主题、布局Provider
- `/src/styles/*` - 全局样式

#### 2. 工具函数
- `/src/lib/utils.ts` - 通用工具函数
- Tailwind配置

### 🗑️ 删除的模块

#### 1. 复杂的后台管理功能
- `/src/features/users/` - 用户管理（删除）
- `/src/features/roles/` - 角色管理（删除）
- `/src/features/permissions/` - 权限管理（删除）
- `/src/features/menu/` - 菜单管理（删除）
- `/src/features/audit-logs/` - 审计日志（删除）
- `/src/features/modules/` - 模块管理（删除）

#### 2. 复杂的认证系统
- `/src/lib/auth/*` - 完整RBAC系统（简化为简单登录）

#### 3. 旧的路由
- `/src/routes/_authenticated/*` - 旧的认证路由（重写）
- `/src/routes/(auth)/*` - 认证页面（简化）

#### 4. 旧的布局
- `/src/components/layout/*` - 后台管理风格布局（重写为SaaS风格）

### 🆕 新增的模块

#### 1. Mock数据层
```
/src/data/
  ├── mock/
  │   ├── properties.ts      # 物业Mock数据
  │   ├── tenancies.ts       # 租约Mock数据
  │   ├── tenants.ts         # 租客Mock数据
  │   ├── maintenance.ts     # 维修工单Mock数据
  │   ├── inspections.ts     # 检查记录Mock数据
  │   ├── financials.ts      # 财务数据Mock
  │   └── ai-insights.ts     # AI洞察Mock数据
```

#### 2. API接口抽象层
```
/src/services/
  ├── api/
  │   ├── client.ts          # HTTP客户端（支持Mock/Real切换）
  │   ├── properties.ts      # 物业API
  │   ├── tenancies.ts       # 租约API
  │   ├── tenants.ts         # 租客API
  │   ├── maintenance.ts     # 维修API
  │   ├── inspections.ts     # 检查API
  │   ├── financials.ts      # 财务API
  │   └── ai.ts              # AI功能API
```

#### 3. 核心功能模块
```
/src/features/
  ├── dashboard/             # 仪表盘
  │   ├── components/
  │   └── pages/
  ├── properties/            # 物业管理
  │   ├── components/
  │   ├── pages/
  │   └── types.ts
  ├── tenancies/             # 租约管理
  ├── tenants/               # 租客管理
  ├── maintenance/           # 维修管理
  ├── inspections/           # 检查管理
  ├── financials/            # 财务管理
  └── ai-features/           # AI差异化功能
      ├── property-pilot/    # AI助手
      ├── compliance-checker/ # 合规检查
      ├── investor-dashboard/ # 投资仪表盘
      ├── tenant-scoring/    # 租客评分
      └── cost-prediction/   # 成本预测
```

#### 4. 新的布局系统（SaaS风格）
```
/src/components/layout-v2/
  ├── app-shell.tsx          # 应用外壳
  ├── sidebar.tsx            # 侧边栏（简洁现代）
  ├── header.tsx             # 顶部栏
  └── command-palette.tsx    # 命令面板（Cmd+K）
```

---

## 🎨 UI/UX设计方向

### 设计参考
- **Linear** - 极简、快速、现代
- **Notion** - 干净、直观
- **Vercel Dashboard** - 优雅、专业

### 核心设计原则
1. **极简主义** - 减少视觉噪音
2. **快速响应** - 所有操作<100ms反馈
3. **智能提示** - 主动帮助用户
4. **数据可视化** - 图表优先

### 色彩方案
- **主色**: Indigo/Blue（专业、信任）
- **辅色**: Green（成功、增长）、Amber（警告）、Red（危险）
- **中性色**: Gray scale（现代感）
- **支持暗色模式**

### 字体
- **标题**: Inter（现代、清晰）
- **正文**: Inter（一致性）
- **代码**: JetBrains Mono（数据展示）

---

## 📊 Mock数据设计原则

### Realistic数据
- 真实的新西兰地址（Auckland, Wellington, Christchurch）
- 真实的租金水平（Auckland: $500-800/周）
- 真实的物业类型（House, Apartment, Townhouse）
- 真实的时间线（最近3年数据）

### 数据量
- 20+ 物业
- 30+ 租约（包含历史租约）
- 50+ 租客
- 100+ 维修工单
- 200+ 财务记录

### 数据关联
- 完整的关系：物业 -> 租约 -> 租客
- 财务记录关联租约
- 维修工单关联物业
- 检查记录关联租约

---

## 🔌 API接口设计（方便后续替换）

### 接口抽象
```typescript
// services/api/client.ts
interface APIClient {
  get<T>(url: string, params?: any): Promise<T>
  post<T>(url: string, data?: any): Promise<T>
  put<T>(url: string, data?: any): Promise<T>
  delete<T>(url: string): Promise<T>
}

// Mock实现
class MockAPIClient implements APIClient {
  async get<T>(url: string): Promise<T> {
    // 从Mock数据返回
  }
}

// 真实实现（后续）
class RealAPIClient implements APIClient {
  async get<T>(url: string): Promise<T> {
    // 从后端API返回
  }
}

// 工厂函数
export const createAPIClient = (): APIClient => {
  return import.meta.env.VITE_USE_MOCK_API === 'true'
    ? new MockAPIClient()
    : new RealAPIClient()
}
```

### 数据格式统一
```typescript
interface APIResponse<T> {
  success: boolean
  data: T
  message?: string
  pagination?: {
    total: number
    page: number
    pageSize: number
    totalPages: number
  }
}
```

---

## 📅 2天开发计划（详细）

### Day 1 - Morning (4小时)

#### Hour 1-2: 清理与准备
- [x] 删除不需要的features模块
- [x] 删除复杂的auth系统
- [x] 保留基础UI组件
- [x] 创建新的目录结构

#### Hour 3-4: Mock数据 + API层
- [ ] 创建realistic Mock数据
- [ ] 实现API接口抽象层
- [ ] 测试Mock数据返回

### Day 1 - Afternoon (4小时)

#### Hour 5-6: 新布局系统
- [ ] 实现现代SaaS风格布局
- [ ] 侧边栏导航
- [ ] 顶部栏
- [ ] 命令面板（Cmd+K）

#### Hour 7-8: Dashboard页面
- [ ] 总览卡片（物业数、租金收入、空置率）
- [ ] 收入趋势图
- [ ] 待办事项列表
- [ ] 近期活动

### Day 1 - Evening (2小时)

#### Hour 9-10: Properties页面
- [ ] 物业列表（卡片/表格切换）
- [ ] 筛选和搜索
- [ ] 物业详情页
- [ ] 基础CRUD

---

### Day 2 - Morning (4小时)

#### Hour 1-2: AI智能助手 PropertyPilot
- [ ] AI助手UI组件（右下角悬浮球）
- [ ] 任务推荐列表
- [ ] 一键执行操作
- [ ] Mock AI建议

#### Hour 3-4: 合规检查系统
- [ ] Healthy Homes合规清单
- [ ] 合规状态可视化
- [ ] 一键生成报告
- [ ] 提醒设置

### Day 2 - Afternoon (4小时)

#### Hour 5-6: 业主投资仪表盘
- [ ] ROI可视化图表
- [ ] 收入vs支出对比
- [ ] 市场对比数据
- [ ] 投资建议

#### Hour 7-8: 租客信用评分
- [ ] 评分算法Mock
- [ ] 评分可视化
- [ ] 历史记录展示
- [ ] 风险评估

### Day 2 - Evening (2小时)

#### Hour 9-10: 最终优化
- [ ] UI细节打磨
- [ ] 动画效果
- [ ] 响应式适配
- [ ] 性能优化
- [ ] 测试所有功能
- [ ] 准备路演演示脚本

---

## ✅ 验收标准

### 功能完整性
- [x] 6大差异化功能全部可演示
- [ ] Mock数据realistic且完整
- [ ] 所有页面可正常导航
- [ ] 无明显Bug

### UI/UX质量
- [ ] 现代SaaS风格
- [ ] 响应式设计
- [ ] 流畅的动画
- [ ] 统一的设计语言

### 技术质量
- [ ] 代码结构清晰
- [ ] API层抽象合理
- [ ] 易于后续扩展
- [ ] 构建无错误

### 路演准备
- [ ] 演示脚本
- [ ] 关键功能流程顺畅
- [ ] 数据展示有说服力
- [ ] 加载速度快

---

## 🚀 下一步

立即开始执行！

**现在开始**: Day 1 - Hour 1 - 清理代码

---

*最后更新: 2025-11-21*
