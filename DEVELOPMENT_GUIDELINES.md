# 项目开发指导方针

## UI 框架和组件选型原则

### 🎨 UI 基础框架

**核心框架**: shadcn/ui

项目的 UI 体系以 **shadcn/ui** 为基础，优先使用 shadcn 提供的组件和模板。

### 📊 表格组件选型策略

#### 1. **默认选择: shadcn Data Table**

对于常规的数据表格需求，**优先使用 shadcn 自带的 data table 组件**。

**适用场景**:
- 简单的数据展示
- 基础的排序、筛选功能
- 标准的 CRUD 操作
- 分页显示
- 行选择
- 响应式表格

**优势**:
- 与项目整体 UI 风格一致
- 轻量级，性能良好
- 易于定制和扩展
- 完整的 TypeScript 支持

#### 2. **增强选择: AG Grid 社区版**

当 shadcn data table **无法满足复杂需求**时，使用 **AG Grid Community Edition**。

**适用场景**:
- 大量数据渲染 (虚拟滚动)
- 复杂的列操作 (拖拽、固定、分组)
- 高级筛选和聚合
- 单元格编辑
- 树形数据展示
- 导出功能 (CSV, Excel)
- 自定义单元格渲染器

**使用原则**:
- ✅ 仅在 shadcn data table 功能不足时使用
- ✅ 保持 AG Grid 的主题风格与 shadcn 一致
- ✅ 使用社区版功能，避免企业版依赖

### 🎯 组件选型决策流程

```
需要实现功能
    ↓
shadcn 是否有现成组件?
    ├─ 是 → 使用 shadcn 组件
    │       └─ 可以通过简单扩展实现? → 扩展 shadcn 组件
    │
    └─ 否 → shadcn 是否有类似的 block/example?
            ├─ 是 → 基于 shadcn block 定制
            │
            └─ 否 → 是否是表格功能?
                    ├─ 是 → 能用 shadcn data table?
                    │       ├─ 是 → 使用 shadcn data table
                    │       └─ 否 → 使用 AG Grid 社区版
                    │
                    └─ 否 → 评估是否需要引入新库
                            (需要讨论和批准)
```

### 📚 shadcn MCP Server

项目已配置 shadcn MCP server，可以通过以下工具快速查找组件:

- `mcp__shadcn__search_items_in_registries` - 搜索组件
- `mcp__shadcn__view_items_in_registries` - 查看组件详情
- `mcp__shadcn__get_item_examples_from_registries` - 获取使用示例

### ✨ 开发建议

1. **保持一致性**: 所有 UI 组件应遵循 shadcn 的设计风格
2. **优先复用**: 在实现新功能前，先查询 shadcn 是否有现成方案
3. **渐进增强**: 从简单的 shadcn 组件开始，确实需要时才升级到 AG Grid
4. **主题统一**: 确保 AG Grid 的样式与 shadcn 主题保持一致

### 📦 当前已使用的核心组件

#### shadcn/ui 组件
- Button, Card, Dialog, Form
- Input, Select, Checkbox, Switch
- Table, Tabs, Tooltip
- Sidebar, Dropdown Menu
- Alert, Badge, Avatar
- Calendar, Popover
- ...等 30+ 组件

#### AG Grid (仅用于复杂表格)
- 用户管理页面 (大量数据 + 复杂筛选)
- 角色管理页面 (需要列固定和排序)
- 权限管理页面 (树形数据展示)

---

## 国际化 (i18n)

### 架构
项目采用**模块化 i18n 系统**:
- 全局翻译: `src/locales/{locale}/*.json`
- 功能模块翻译: `src/features/{module}/locales/{locale}.json`

### 使用方式
```typescript
import { useI18n } from '@/lib/i18n'

const { t } = useI18n()

// 全局翻译
t('common.save')
t('nav.dashboard')

// 功能模块翻译
t('users.title')
t('settings.description')
```

### 添加新翻译
1. 在对应的功能模块目录下创建 `locales/` 文件夹
2. 添加 `en.json` 和 `zh-CN.json` 文件
3. 翻译自动加载，无需配置

详见: `I18N_MODULAR_GUIDE.md` 和 `I18N_QUICK_START.md`

---

## 代码规范

### 组件导入顺序
```typescript
// 1. React 相关
import { useState, useEffect } from 'react'

// 2. 第三方库
import { toast } from 'sonner'

// 3. UI 组件
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'

// 4. 项目组件和工具
import { useI18n } from '@/lib/i18n'
import { UserDialog } from '@/features/users/components/user-dialog'

// 5. 类型定义
import type { User } from '@/features/users/types'
```

### 文件命名
- 组件: `kebab-case.tsx` (如 `user-dialog.tsx`)
- 工具函数: `kebab-case.ts` (如 `format-date.ts`)
- 类型: `types.ts` 或 `index.ts`

---

**最后更新**: 2025-10-17
**维护者**: AI Assistant (Claude)
